[project]
name = "hyperate-overlay"
version = "1.0.0"
description = "Hyperate 三值置顶悬浮窗 - 实时显示当前/最高/最低心率的透明悬浮窗应用"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "requests>=2.32.5",
    "websockets>=15.0.1",
    "python-dotenv>=1.0.0",
]
