# Hyperate Triple Overlay 环境变量配置
# 复制此文件为 .env 并填写你的配置

# 你的 Hyperate 会话链接（浏览器能看到心率的那个）
# 示例: https://www.hyperate.io/pulse-dynamics-ecg?id=YOUR_ID_HERE
HYPERATE_URL=https://www.hyperate.io/pulse-dynamics-ecg?id=YOUR_ID_HERE

# 显示设置（可选，使用默认值即可）
CURRENT_SIZE=96
CURRENT_COLOR=#FF2D00
MAX_COLOR=#FF6B6B
MIN_COLOR=#4ECDC4
BPM_COLOR=#FFFFFF
BG_TRANSPARENT=true
OPACITY=0.85
UPDATE_INTERVAL=3
POS_X=50
POS_Y=30
BLINK_ENABLE=true
BLINK_THRESHOLD=160
ROW_SPACING=0

# RTSS集成设置（可选）
# RTSS显示格式，可用变量: {current}, {max}, {min}
RTSS_DISPLAY_FORMAT=BPM {current} ({max}/{min})
# RTSS更新间隔（秒）
RTSS_UPDATE_INTERVAL=1
# 显示模式: both(同时显示), default(仅默认UI), rtss(仅RTSS)
DISPLAY_MODE=both
